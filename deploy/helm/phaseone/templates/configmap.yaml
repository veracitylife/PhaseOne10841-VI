apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "phaseone.configMapName" . }}
  labels:
    {{- include "phaseone.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
