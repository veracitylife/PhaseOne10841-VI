apiVersion: apps/v1
kind: Deployment
metadata:
  name: phaseone-dashboard
spec:
  template:
    spec:
      containers:
        - name: dashboard
          resources:
            limits:
              cpu: "1"
              memory: 512Mi
            requests:
              cpu: 200m
              memory: 256Mi
